/* import { localforage } from 'localforage-nuxt';

let $localforage: localforage;

export function initializeLocalforage(axiosInstance: NuxtAxiosInstance) {
  $axios = axiosInstance;
}

export { $axios };
 */
